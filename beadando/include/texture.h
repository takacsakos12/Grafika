#ifndef TEXTURE_H
#define TEXTURE_H

#include <GL/gl.h>

/* BMP textura betoltese OpenGL texturakent, es a textura azonositojanak visszaadasa. */
GLuint load_texture(const char* filename);

#endif /* TEXTURE_H */